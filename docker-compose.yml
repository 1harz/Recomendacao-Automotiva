version: '3.8'

services:
  api:
    build: .
    container_name: automotive-recommender
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
